# חשבונות בית

אפליקציית web לחישוב חלוקת חשבונות מים וחשמל ולצפייה היסטורית, מגובה ב-Supabase.
עורך יחיד (התחברות במייל) מזין; המשפחה צופה דרך הלינק (קריאה בלבד).

הנתונים נשמרים ב-Supabase. הקובץ `config.js` מכיל את כתובת הפרויקט ואת ה-anon key הציבורי
(מוגן ע"י RLS — אנונימי יכול לקרוא בלבד).
